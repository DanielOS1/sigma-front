export const environment = {
    production: true,
    // Reemplazar por la URL real del backend en el entorno de producción.
    // Vacío = las peticiones son relativas al origen donde se sirve el frontend.
    API_URL: ""
};
